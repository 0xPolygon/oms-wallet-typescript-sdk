---
---

Internal workspace restructure and adoption of the changesets release paradigm. No consumer-facing changes: package names, versions, exports, and published contents are unchanged. Both publishable packages (`@polygonlabs/oms-wallet` and `@polygonlabs/oms-wallet-wagmi-connector`) are now a `fixed` changesets group and will always release on the same version.
