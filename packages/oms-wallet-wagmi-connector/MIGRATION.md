# Migration Guide

This document records breaking changes and the steps to migrate between major
versions of `@polygonlabs/oms-wallet-wagmi-connector`.

<!-- No breaking migrations yet. Add a section per major version as they ship. -->
